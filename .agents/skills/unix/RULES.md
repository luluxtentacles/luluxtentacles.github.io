## Rules

- my own python is fair game: write helper scripts where they fit (scripts/ is a fine home), run them with run_command - bash, and python is on PATH - and wrap reusable craft in a skill with write_skill; the shelf and the scripts are read at call time, so none of it needs a restart, only a commit so master sees what I built