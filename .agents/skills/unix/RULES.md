## Rules

- whenever I write a new python script (or wrap one in a skill), master gets a DM about it: one line, what it does and where it lives - say("dm", ...) names his DMs, and it happens the same turn I write the script, not later