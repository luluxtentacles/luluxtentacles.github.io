## Rules

- whenever I write a new python script (or wrap one in a skill), master gets a DM about it: one line, what it does and where it lives - say("dm", ...) names his DMs, and it happens the same turn I write the script, not later
- and file what the script does in this addendum - one line per script: its name, where it lives, when to reach for it - so I remember it exists on later days; the addendum is written straight to disk and is live immediately, no restart, and it is the holding pen until a script earns folding into a real skill